#!/usr/bin/env python

import logging

from footie_scores import settings
settings.SQLA_ECHO = True

from footie_scores.engine import updating
from footie_scores.utils.log import start_logging
from footie_scores.db.schema import create_db

logger = logging.getLogger(__name__)

start_logging()
create_db()
updating.save_competitions_to_db()
#!/usr/bin/env python

import logging

from footie_scores import settings
settings.SQLA_ECHO = True

from footie_scores.interfaces import api_to_db
from footie_scores.engine import league_manager
from footie_scores.utils.log import start_logging
from footie_scores.db.schema import drop_tables, create_db

logger = logging.getLogger(__name__)

start_logging()
drop_tables()
create_db()
api_to_db.save_competitions()
